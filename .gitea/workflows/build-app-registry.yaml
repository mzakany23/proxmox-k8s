name: Build and Deploy App Registry

on:
  push:
    branches:
      - main
    paths:
      - 'kubernetes/apps/app-registry/**'
      - '.gitea/workflows/build-app-registry.yaml'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./kubernetes/apps/app-registry
          push: true
          tags: |
            docker-registry.container-registry.svc.cluster.local:5000/homelab/app-registry:latest
            docker-registry.container-registry.svc.cluster.local:5000/homelab/app-registry:${{ github.sha }}
          cache-from: type=registry,ref=docker-registry.container-registry.svc.cluster.local:5000/homelab/app-registry:buildcache
          cache-to: type=registry,ref=docker-registry.container-registry.svc.cluster.local:5000/homelab/app-registry:buildcache,mode=max

      - name: Trigger ArgoCD Sync
        run: |
          kubectl patch application app-registry -n argocd --type merge -p '{"operation":{"initiatedBy":{"username":"gitea-actions"},"sync":{}}}'
