apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Centralized SealedSecret resources
# These are created with kubeseal and committed here
# Sync wave: 2 (after core infrastructure, before platform services)

# Note: Most secrets are currently managed inline with their applications
# or created via kubeseal and applied directly. Add SealedSecret files here
# as they get consolidated.

resources: []
# Example:
# - gitea-secrets.yaml
# - registry-secrets.yaml
# - database-credentials.yaml
