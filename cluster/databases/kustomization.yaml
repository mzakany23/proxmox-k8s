apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Stateful database services (sync-wave: 4)
# Deployed after platform services, before user apps

resources:
- conversation-history-postgres/
- milvus/
