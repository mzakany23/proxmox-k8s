apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Milvus - Vector database for embeddings
# Includes: etcd (metadata), minio (storage), milvus (vector DB)

resources:
- etcd-statefulset.yaml
- minio-statefulset.yaml
- milvus-config.yaml
- milvus-statefulset.yaml
- milvus-ingress.yaml
