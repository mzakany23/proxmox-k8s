apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Platform services - shared infrastructure (sync-wave: 3)
# Deployed after core infrastructure and secrets

resources:
- gitea/
- gitea-actions/
- monitoring/
- registry/
- vm-proxies/
