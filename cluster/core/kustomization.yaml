apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Core cluster infrastructure - deploys first (sync-wave: 1)
# These components are required for the cluster to function

resources:
- metallb/
- ingress-nginx/install.yaml
- cert-manager/
- sealed-secrets/sealed-secrets.yaml
- monitoring/  # node-exporter, kube-state-metrics
- argocd/      # ArgoCD server ingress
