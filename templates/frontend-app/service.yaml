apiVersion: v1
kind: Service
metadata:
  name: REPLACE_APP_NAME
  namespace: default
  labels:
    app: REPLACE_APP_NAME
spec:
  type: ClusterIP
  selector:
    app: REPLACE_APP_NAME
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
