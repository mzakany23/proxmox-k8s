apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  # No ingress - backend services are internal only

# Optional: Use kustomize to manage multiple environments
# commonLabels:
#   environment: production
#   team: platform

# namePrefix: prod-
# namespace: production
